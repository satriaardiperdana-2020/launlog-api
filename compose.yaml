services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: launlog
      POSTGRES_USER: launlog
      POSTGRES_PASSWORD: launlog
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
